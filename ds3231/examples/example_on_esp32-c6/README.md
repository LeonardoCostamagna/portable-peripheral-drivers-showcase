# ESP32-C6 Example Application

---

The `examples/example_on_esp32-c6/` directory demonstrates how to integrate the driver into an ESP-IDF project.

This application serves as a demonstration project showcasing how to integrate and utilize the DS3231 RTC driver on an **ESP32-C6**. Key functionalities include:

* **I2C Bus Configuration:** Initializes the ESP32-C6 master I2C bus and registers the DS3231 as a slave device.
* **Hardware Interrupt Handling:** Configures the DS3231's Square-Wave (SQW) output pin as a GPIO interrupt, utilizing a FreeRTOS Queue to handle events within an Interrupt Service Routine (ISR).
* **Driver Integration:** Demonstrates the driver’s dependency injection pattern by providing platform-specific I2C read/write callback functions.
* **Time and Temperature Management:** Performs initial RTC synchronization and enters a continuous loop to retrieve and log the current date, time, and the DS3231's internal temperature sensor readings.

## Prerequisites

To build and run the example, you need:

* **ESP-IDF environment:** Host development environment provided by Espressif.
* **ESP32-C6-DevKitC-1:** Development board.

## Hardware Connections

| Signal / Pin | ESP32-C6 Pin | Description |
| :--- | :--- | :--- |
| **DS3231 SCL** | GPIO 7 | I2C Clock Line |
| **DS3231 SDA** | GPIO 11 | I2C Data Line |
| **DS3231 SQW** | GPIO 10 | Square Wave / Interrupt Output |

## How to Build and Flash

1. Open a terminal and navigate to the example directory:
```bash
cd examples/example_on_esp32-c6
```

2. Set up your ESP-IDF environment variables (if not already loaded):

```bash
. $HOME/esp/esp-idf/export.sh
```

> **Note:** This path is generic. Make sure to adjust it to match the actual location of the export.sh script on your host machine.

* This command loads the toolchains, paths, and environment variables required to use the ESP-IDF framework in your current terminal session.

3. Build the project:

```bash
idf.py set-target esp32c6
idf.py build
```

* `idf.py set-target esp32c6`: Configures the project for the target microcontroller architecture (in this case, the ESP32-C6).
* `idf.py build`: Compiles the source code, links libraries, and generates the binary firmware image.

4. Flash the firmware and monitor the serial output:

```bash
idf.py -p /dev/ttyUSB0 flash monitor
```

> **Note:** Replace `/dev/ttyUSB0` with your actual port if necessary.

* `flash`: Uploads the compiled binary firmware to the connected development board over the specified serial port.
* `monitor`: Opens the serial console to view real-time logs output generated by the device.

## Expected Output

Once running, you will observe the device logging the incremental time update based on the configured date and time, alongside temperature readings from the DS3231 internal sensor:

```text
I (254) main_task: Calling app_main()
I (254) MAIN: Initializing DS3231 Demo on ESP32-C6...
I (254) MAIN: DS3231 Driver successfully initialized
I (254) MAIN: Time set to 2026-08-08 12:00:00
I (264) MAIN: RTC Time: 2026-08-08 12:00:00
I (264) MAIN: Temperature: 20.75 degC
I (2264) MAIN: RTC Time: 2026-08-08 12:00:02
I (2264) MAIN: Temperature: 20.75 degC
I (4264) MAIN: RTC Time: 2026-08-08 12:00:04
I (4264) MAIN: Temperature: 20.75 degC
I (6264) MAIN: RTC Time: 2026-08-08 12:00:06
I (6264) MAIN: Temperature: 20.75 degC
I (8264) MAIN: RTC Time: 2026-08-08 12:00:08
I (8264) MAIN: Temperature: 20.75 degC
I (10264) MAIN: RTC Time: 2026-08-08 12:00:10
I (10264) MAIN: Temperature: 20.75 degC
```

---